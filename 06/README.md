# 第 6 章　对象引用、可变性和垃圾 回收



## 6.3　同一性、相等性和别名



* `is` 运算符比较两个对象的标识，`id()` 函数返回对象标 识的整数表示



**6.3.1　在 == 和 is 之间选择**

* `==` 运算符比较两个对象的值
* is 运算符比 == 速度快，因为它不能重载
* `__eq__`



**6.3.2　元组的相对不可变性**



## 6.4 默认做浅拷贝

* copy 模块提供的 copy 和 deepcopy 函数 分别对任意对象做浅拷贝和深拷贝。
* `__copy__`，`__deepcopy__`

## 6.5　函数的参数是引用时



**6.5.1　不要使用可变类型作为参数的默认值**





## 6.6　del 和垃圾回收



* 即将销毁实例时，Python 解释器 调用 __del__ 方法，给实例最后的机会释放外部资源
* 对象的弱引用不增加对象的引用计数。 因此，弱引用不阻碍目标对象被当作垃圾而回收



## 6.7　Python 对不可变类型施加的把戏